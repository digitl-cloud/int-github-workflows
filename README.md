> [!IMPORTANT]
> **Deprecated / archived.** The repos that used these reusable GitHub Actions
> workflows have migrated to GitLab CI (inline, tag-driven pipelines). This
> repository is no longer used.

